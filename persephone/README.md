# Persephone

The soft edge keeper of DAWN.

## Core Functions

| Function | Description |
|----------|-------------|
| `fade_sigils()` | Decays symbolic heat and sigil pressure |
| `prune_dead_branches()` | Deletes semantic debris and orphaned memory paths |
| `soft_seal_bloom(flower)` | Archives stable Juliet blooms |
| `expire_ephemeral_sigils()` | Clears expired symbols or commands |
| `seed_whisper()` | Triggers rebloom opportunities |
| `balance_ring_entropy()` | Normalizes sigil distribution under pressure |
